Product Deck — Node backend + Vite frontend (updated)
This package includes:
- backend with CORS enabled and robust JSON handling
- frontend with defensive fetch and Vite proxy config
- docker-compose (same as before) (not rebuilt here)
- Run locally:
  cd backend
  npm install
  npm start
  cd frontend
  npm install
  npm run dev
  Open http://localhost:3000
